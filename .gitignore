# Secrets — never commit
include/settings.inc
# Google service-account key(s) — never commit; on servers keep OUTSIDE the web root
find-a-game-207620-ddabf9831915.json
google-sa*.json
/*.json

# Logs
*.log
revision.log
vssmove.log

# Database dumps (root-level only; sql/ migration files are committed)
/*.sql
approvals_2017.sql

# Backups and junk (date-stamped, .bak, .debug, .temp variants)
*.bak
*.temp
*.debug
*-backup
*-backup*
*.20[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]
*.20[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]-*
MES Membership Handbook 2025.md

# Runtime-generated directories
backups/
cache/
templates_c/

# Submodule (managed separately)
PHP-OAuth2

# PHPUnit (dev-only test framework; fetch the phar via tools/get-phpunit.sh|.ps1)
/tools/phpunit.phar
/.phpunit.cache/
/.phpunit.result.cache
